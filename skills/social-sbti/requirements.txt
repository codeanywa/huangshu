# skill 核心(match / make_card / personalities / cli / config / doctor)是零依赖的标准库。
# 下面三个是抓取 + 渲染必需的,首次使用 sbti doctor --fix 会自动装。

# 即刻 fetcher(必需)
jike-skill[qr]>=0.1.0

# X/Twitter fetcher(必需)
twikit>=2.0.0

# HTML → PNG 截图(必需)
playwright>=1.40.0
# 首次装完后再跑一次: playwright install chromium
